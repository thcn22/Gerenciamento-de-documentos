# File upload limits
MAX_FILE_SIZE=50MB
UPLOAD_DIR=uploads

# Database configuration (if using external database)
# DATABASE_URL=postgresql://user:password@localhost:5432/docmanager

# Email configuration (for sending real emails)
# Leave empty to use development mode (emails shown in console)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=
EMAIL_PASS=
FROM_EMAIL=noreply@docmanager.com
FROM_NAME=DocManager System

# Frontend URL for email links
FRONTEND_URL=http://localhost:8080
